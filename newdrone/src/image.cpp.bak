#include <graphics.h>
#include "image.h"
#include <alloc.h>
#include <stdio.h>
Layer::Layer(int ix1, int iy1, int ix2, int iy2)
{
    x1 = ix1;
    y1 = iy1;
    x2 = ix2;
    y2 = iy2;
    int imsize;
    imsize = imagesize(x1, y1, y2, y2);
    figure_layer = (void far *) farmalloc(imsize);
    getimage(x1, y1, x2, y2, figure_layer);
}

int Layer::putlayer(int ix1, int iy1, int ix2, int iy2)
{
    int i, j, pix = 0;
    pix =  iy1 * (x2 - x1 + 1);
    pix += ix1;
    printf("%d", figure_layer[100][100]);
    for(j=0; j<= iy2; j++)
    {
        pix += (x2 - x1 + 1);
        for(i=0; i <= ix2; i++)
        {
			//putpixel(ix1+i, iy1+j, far *(figure_layer + pix));
        }
    }
}